# Governance package (Phase 0).
#
# Guardrails are load-bearing: allowlist enforcement, purpose binding, audit,
# retention and the generated notice are imported by the ingest and API layers
# rather than living alongside them as advisory documentation.
